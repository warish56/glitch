import {appColors} from './colors';
import {componentsColors} from './components';
import {bodyFontSize, titleFontSize} from './fontSize';

export const THEME = {
  colors: appColors,
  components: componentsColors,
  fontSize: {
    title: titleFontSize,
    body: bodyFontSize,
  },
};
