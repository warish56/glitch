import {appColors} from './colors';
import {bodyFontSize, titleFontSize} from './fontSize';

export const THEME = {
  colors: appColors,
  fontSize: {
    title: titleFontSize,
    body: bodyFontSize,
  },
};
